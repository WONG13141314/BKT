services:
  # Frontend - Static Site
  - type: web
    name: monomath
    runtime: static
    buildCommand: npm install --include=dev && npm run build --workspace=frontend
    staticPublishPath: frontend/dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false
      - key: VITE_SOCKET_URL
        sync: false

  # Backend - Web Service
  - type: web
    name: monomath-api
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && cd backend && npx prisma generate && npm run build
    startCommand: cd backend && npx prisma migrate deploy && npx tsx src/server.ts
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: CORS_ORIGIN
        sync: false
      - key: DATABASE_URL
        sync: false
