// Game feature — Dice roll UI with animation

export function DiceRoller() {
  return (
    <div className="dice-roller">
      {/* TODO: Two dice with roll animation, emit roll event via socket */}
    </div>
  );
}
