// Hook for Socket.IO game event listeners
// Subscribes to server-broadcast game state updates

export function useGameSocket() {
  // TODO: Listen to game state updates, dice results, turn changes, etc.
}
