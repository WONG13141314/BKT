// Game REST API service — create/join game sessions
// TODO: POST /api/games, GET /api/games/:id

export const gameService = {
  createGame: async () => {
    // TODO: POST /api/games
  },
  joinGame: async (_gameId: string) => {
    // TODO: POST /api/games/:id/join
  },
  getGame: async (_gameId: string) => {
    // TODO: GET /api/games/:id
  },
};
